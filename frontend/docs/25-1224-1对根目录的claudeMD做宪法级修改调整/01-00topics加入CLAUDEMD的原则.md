结论先给：
**✅ `/t/`（Topic Hub）可以、而且应该加入 `CLAUDE.md`，但只能以「Priority 1.5 · 语义实体定义（非教学、非路径）」的方式加入，位置与表述非常关键。**
如果你放错位置或写重了，它会**反向污染 Program / Path / Skill 的语义主权**。

下面我分三步给你**可直接落地的方案**。

---

## 一、`/t/` 在你整个宪法体系里的“正确身份”

你现在给 `/t/` 的定义是对的，但我要先**用宪法语言帮你定性**，否则后面写不稳。

### `/t/` 的宪法级身份（一句话）

> **`/t/` 是“语义主题枢纽（Topic Hub）”，不是教学结构实体，也不是能力实体。**

对应你体系中的位置：

| 维度            | 是否拥有 |
| ------------- | ---- |
| 教学顺序          | ❌    |
| 学习阶段（Level）   | ❌    |
| 教学结构主权        | ❌    |
| 能力抽象主权（Skill） | ❌    |
| 主题语义主权        | ✅    |
| 内链与聚合主权       | ✅    |

👉 这意味着：
**`/t/` 的“权力”只存在于语义聚合层，不得越权到教学层或能力层。**

---

## 二、`/t/` 应该加在 CLAUDE.md 的哪个位置？（很关键）

### ❌ 绝对不要这样做

* ❌ 不要把 `/t/` 加进 **Priority 1 / Skill / Program 那一节**
* ❌ 不要把 `/t/` 写成和 Skill / Program 并列的“学习实体”
* ❌ 不要写任何“推荐顺序”“学习建议”

### ✅ 正确位置（两种都可以）

### 方案 A（**推荐**）：

👉 放在 **Appendix · AEO Entity Expansion Plan（预案）** 中
作为 **Priority 1.5：语义聚合实体**

这是**最安全、最宪法级**的方式。

---

## 三、给你一段【可直接粘进 CLAUDE.md】的标准宪法文本

> 你可以 **原样复制**，这是已经“去风险化”的版本。

---

### 🔹 在 Appendix 中新增一节（推荐写法）

```md
### Priority 1.5 — Topic Hub（/t/，语义聚合实体 · 非教学）

**`/t/`（Topic Hub）** 是主题级语义聚合实体，用于：
- 跨 Course / Skill / HowTo / FAQ 的主题归档
- 构建稳定的主题语义锚点
- 作为站内内链与语义关联的中心节点

#### 语义边界（强制）

- `/t/` **不定义** 学习阶段（Level）
- `/t/` **不定义** 学习顺序或学习路径
- `/t/` **不具备** 教学结构主权
- `/t/` **不得表现为** Program / Path / Skill

#### 允许的职责

- ✅ 按“主题相关性”聚合内容
- ✅ 作为内链与语义推荐中心
- ✅ 关联 Course / Skill / HowTo / FAQ（非顺序、非依赖）

#### 禁止的行为（FORBIDDEN）

- ❌ 使用 `hasPart` 表达教学结构
- ❌ 表达先学 / 后学、阶段划分
- ❌ 替代或弱化 Program / Path 的结构主权
- ❌ 在 `/t/` 中解释 Beginner / Intermediate / Advanced 含义

#### AEO / Schema 原则

- `/t/` 仅作为 **主题聚合语义节点**
- Schema 类型应使用 `Thing` 或轻量语义类型
- 不得输出 Course / EducationalOccupationalProgram / DefinedTermSet
```

---

## 四、为什么这个写法是“宪法级安全”的？

我直接告诉你**如果不这么写，会出什么事**：

### ❌ 如果你把 `/t/` 写成“学习实体”

* AI 会开始在 `/t/` 里塞课程顺序
* Program 的 hasPart 会被稀释
* 搜索引擎会混淆“主题页 vs 体系课页”

### ❌ 如果你不给 `/t/` 明确禁止 hasPart

* AI 会下意识把 `/t/` 当“集合容器”
* 最终变成“第二个 Program（但没顺序）”

### ✅ 现在这版的好处

* `/t/` **有实体身份，但没有教学权力**
* 能支撑 **SEO / AEO 的主题权重**
* 不会污染你已经非常干净的
  **Course → Program → Path → Level 主干结构**

---

## 五、你接下来可以怎么做（非常重要）

1️⃣ **现在就把上面那段加进 `CLAUDE.md` 的 Appendix**
2️⃣ 不要急着实现 `/t/` 页面
3️⃣ 等你 Course / Skill 数量到一定规模，再激活 `/t/` 的内链价值

---

## Summary

`/t/` 可以加入 `CLAUDE.md`，但**只能以“Priority 1.5 · 语义主题聚合实体”加入 Appendix**。
它只负责主题与内链，不得触碰教学顺序、阶段或能力主权。

## In short（中文）

把 `/t/` 作为 **Priority 1.5 的 Topic Hub** 写进 `CLAUDE.md` 的 Appendix，
只允许“主题聚合 + 内链”，**严禁教学顺序与阶段语义**。
我已经给了你一段**可直接粘贴的宪法级文本**。
