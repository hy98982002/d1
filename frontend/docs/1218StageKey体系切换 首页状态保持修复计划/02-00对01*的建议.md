我已经把 **00***（合并版）和 **01***（在 00* 基础上的进一步建议）当成**一次真正的架构评审**完整看过了，下面给你**结论级判断 + 明确行动建议**，不绕。

---

## 一、问题 1：00* 是合并版，这个“基线”稳不稳？

### ✅ 结论：**00* 可以作为“执行基线（baseline）”**

从工程与语义两个层面看：

* ✅ StageKey 从 `basic → beginner` 的切换是**完整的**
* ✅ 首页状态错乱问题的根因定位**准确**
* ✅ 修改顺序（类型 → 常量 → store → 组件）是**工程正确顺序**
* ✅ 不触碰 SEO / JSON-LD 决策边界，**符合 CLAUDE.md 宪法**

👉 **00*** 的定位非常清晰：

> **“解决当下 bug + 完成体系切换的最小闭环”**

这一点是对的。

---

## 二、问题 2：01*（对 00* 的进一步建议）评测

我给你一个非常清晰的判断：

### ✅ 01* **不是“推翻 00*”，而是“把 00* 提升到长期可维护级别”**

你可以把两者理解为：

| 文档  | 角色                   |
| --- | -------------------- |
| 00* | **最低可交付方案（MVP Fix）** |
| 01* | **工程化 / AEO 友好增强版**  |

---

### 01* 中 **特别值得保留的 5 点（全部是加分项）**

#### 1️⃣ 提出 **批量清理残留 `basic`** 的意识（非常关键）

> 不是“代码能跑就行”，而是**语义不留垃圾**

这是**长期 AEO 项目**才会有的意识，必须肯定。

---

#### 2️⃣ 提议抽 `useStageSync()` composable

这是**架构级进化**：

* 避免每个页面都写 watch
* 为未来 `/levels`、列表页、program 页复用做准备

👉 **这一步不是必须立刻做，但非常值得做**

---

#### 3️⃣ 把 `stage` 纳入 **URL / query / 深链恢复** 的视角

这是 **AEO + UX 的交叉点**：

* `/course/xxx?stage=advanced`
* `/` 首页直接 deep link 到某一层级

👉 这一步 **非常有利于 AEO / 分享 / 搜索复现**

---

#### 4️⃣ 提到 **持久化（sessionStorage）**

这不是 UX 小优化，而是：

> **“用户意图的短期记忆”**

对于搜索引擎 + 用户回访路径都很友好。

---

#### 5️⃣ 明确 **JSON-LD / /levels canonical 的同步问题**

说明你已经在用**“实体视角”**看这个问题，而不是前端 bug。

---

### ❗ 唯一需要你“心里有数”的一点

> **01*** 的前提是：
> **你已经接受这是“体系长期演进工程”，而不是一次性修 bug**

如果你现在的目标是：

* 🟡 先跑通 → 可以只做 00*
* 🟢 准备长期 AEO → **01* 里的增强点一定要逐步吃掉**

---

### 🔚 对问题 2 的最终结论

> **01* 的建议是正确的、前瞻的、不会与未来冲突。**
> 建议你把它当成：
> **“00* 执行完成后的 Phase-2 清单”**

---

