我直接给你**标准化、一次到位的处理方案**。这是**可复制为长期规则**的做法，不是临时建议。

---

## ✅ 总原则（先给结论）

**是的：**

* 👉 **这些 AI 强制执行 Prompt**
  **必须存在于 `/docs/ai/`**
* 👉 **必须在 `/CLAUDE.md` 中“简要引用 + 授权其强制生效”**
* ❌ **绝不能把 Prompt 正文复制进 `/CLAUDE.md`**

这是你当前文档宪法体系下**唯一合宪**的做法。

---

## 一、这些 Prompt 在体系中的“法律地位”

你现在已经有三层：

| 层级  | 文件                | 职责                   |
| --- | ----------------- | -------------------- |
| 宪法  | `/CLAUDE.md`      | 定义什么是合法规则、谁有裁决权      |
| 执法令 | `/docs/ai/*.md`   | 定义 AI 在执行时**必须检查什么** |
| 过程  | `/frontend/docs/` | PRD / 试错 / 推演        |

这类文件（例如 Program / Course Guard）**明确属于第二层：执法令**
👉 **它们不是“规则定义”，而是“规则执行 Gate”**

---

## 二、正确的文件放置方式（你照着做即可）

### 1️⃣ 文件位置（强制）

把每一个 Guard Prompt 放在：

```
/docs/ai/
```

推荐命名规范（你现在已经很接近正确）：

```
docs/ai/
├─ LEVEL_INTEGRITY_EXECUTION_PROMPT.md
├─ PROGRAM_COURSE_GUARD.md
├─ SKILL_ENTITY_GUARD.md（将来）
```

你刚给的这个文件内容，就是一个 **PROGRAM / COURSE Guard**，完全合格 。

---

## 三、/CLAUDE.md 里应该怎么“引用”（重点）

### ❗只做一件事：**声明它是强制执行 Gate**

在你现在 `/CLAUDE.md` 的这一节👇（位置是正确的）：

```md
## 6. AI & Agent Execution Rules（AI 行为红线）
```

**新增一个极短的小节（不要复制 Prompt 内容）：**

```md
### Execution Guards（强制执行 Gate）

For any AI task involving semantic entities or structure,
the following execution guards are MANDATORY and enforceable:

- Level integrity → `docs/ai/LEVEL_INTEGRITY_EXECUTION_PROMPT.md`
- Program / Course boundary → `docs/ai/PROGRAM_COURSE_GUARD.md`

If any guard fails, the AI MUST STOP execution and request human review.
```

👉 **到此为止，不多写一个字。**

---

## 四、为什么不能把 Prompt 正文塞进宪法（再次确认）

如果你把下面这种内容直接放进 `/CLAUDE.md`：

```
Before any change involving Program or Course, you MUST verify...
```

会产生 3 个严重问题：

1️⃣ **宪法层被执行细节污染**
2️⃣ 将来你改 Prompt（≤10 行优化）会被误判为“宪法变更”
3️⃣ AI 会把“检查清单”当成“语义定义”

👉 你自己在 §0 已经明确禁止这种事。

---

## 五、最终执行清单（你照这个核对）

**你现在应该做到：**

* [x] Guard Prompt 单独存在 `/docs/ai/*.md`
* [x] `/CLAUDE.md` 只引用文件路径 + 强制力
* [x] 不复制、不展开、不解释 Prompt 正文
* [x] Guard 失败 → STOP（这一点必须写在宪法里）

---

## 一句话总结（你可以记住）

> **/CLAUDE.md = 立法
> /docs/ai/*.md = 执法令
> AI Prompt = 执行 Gate**

