我直接给你**结论先行 + 可执行判断**，不绕。

---

## ✅ 结论（先给判断）

> **07-01*** 可以**执行**，但**不应直接按 07-00* 原样重构**。
> 需要 **「减 1 条、加 2 条、调 1 个等级」**，否则会出现**误杀过多 + CI 阻断过早**的问题。

下面是**逐条评测 + 必须调整点**，这是“是否可执行”的真实答案。

---

## 一、整体评测（是否具备执行条件）

### 从工程角度看（Node.js lint / CI）

✅ **可执行**

* 规则全部是**结构级**（regex / schema / relationship）
* 不依赖 LLM、不依赖运行时状态
* 符合你“误杀可接受、漏检不可接受”的原则

### 从宪法 / Guard 体系看

⚠️ **需要微调定位**

* 现在 07-00* 更像 **“硬 Guard + lint 混合体”**
* 必须明确哪些是 **ERROR（阻断）**，哪些只能 **WARN**

否则会破坏你已经建立的：

> **CLAUDE.md（语义主权）
> → Guard（执行前 Gate）
> → aeo-lint（工程级扫描）**

---

## 二、逐条评测 07-00* 的规则（是否保留 / 调整）

我按你现在的编号来，不重排，只判断。

---

### ✅ L1 — Level Canonical URL（保留，ERROR）

```text
?stage= / #stage
/levels/(?!beginner|intermediate|advanced)
```

**判断**：
✅ **必须保留，且必须 ERROR**

原因：

* 这是**结构上不可能正确**
* 命中即代表“语义已经被污染”
* 非业务判断，不存在例外

👉 **结论**：不动

---

### ✅ L2 — JSON-LD Level 表达（保留，ERROR）

```json
educationalLevel → DefinedTerm + /levels/*
```

**判断**：
✅ **必须保留，且必须 ERROR**

原因：

* 这是你 AEO / LRMI 的核心主权
* string / enum 一旦放进索引，后果不可逆

👉 **结论**：不动

---

### ⚠️ L3 — Level 中心性（⚠️ 必须降级为 WARN）

```text
Level 页面必须至少被一个非 course 页面链接
```

**问题在这里** ❗

* 这是**结构完整性建议**
* 不是“必然错误”
* 在以下情况会被误杀：

  * 新建 Level 页面但首页尚未上线
  * 分支开发中
  * 灰度 / staging

👉 **调整建议（必须）**：

| 项  | 调整               |
| -- | ---------------- |
| 等级 | ❌ ERROR → ✅ WARN |
| 行为 | 只报告，不阻断 CI       |
| 说明 | “结构风险，而非违规”      |

---

### ✅ P1 — Program / Course 关系（保留，ERROR）

```text
Course ❌ hasPart
Program ✅ hasPart
Topic ❌ hasPart
```

**判断**：
✅ **这是你体系里最重要的一条之一**

原因：

* 这是“教学结构主权”
* 一旦 Course 有顺序，整个知识图谱崩塌

👉 **结论**：不动，ERROR

---

### ⚠️ C1 — Course 原子性（建议保留，但只 WARN）

```text
position / step / 第X阶段
```

**问题**：

* 这是 **“语义信号”**，不是结构必然错误
* 有些课程内容里可能是：

  * “第 1 阶段作业”
  * “阶段性练习”（但不是教学顺序）

👉 **调整建议**：

| 项  | 调整                   |
| -- | -------------------- |
| 等级 | ⚠️ WARN（默认）          |
| 升级 | 连续多次命中 → ERROR（人工策略） |
| 备注 | 需要人工 review          |

---

### ✅ UI1 — UI 状态不得进入语义层（保留，ERROR）

```text
"stage": in JSON-LD
canonical + query
```

**判断**：
✅ **必须保留，且 ERROR**

原因：

* UI state 进入 Schema = 搜索引擎层面的不可逆污染
* 这是“非教学状态渗透语义层”的铁红线

👉 **结论**：不动

---

## 三、**必须新增的 2 条规则（现在 07-00* 缺的）**

这是重点，不加这两条，你的 Guard + lint **不闭环**。

---

### ➕ NEW RULE N1 — Level @id 跨语言稳定性（ERROR）

你宪法里已经写了：

> Level @id must remain stable across languages

但 **lint 里没覆盖**。

#### 检测逻辑（可做）：

* JSON-LD 中出现：

  * `/zh/levels/beginner`
  * `/en/levels/beginner`
* 或同一文件出现多个 levels 域名

👉 **结论**：
这是**结构可检测的**，而且是 **ERROR 级**

---

### ➕ NEW RULE N2 — Topic / Tag 禁止 isPartOf Program（ERROR）

你现在只禁止了：

```text
Topic ❌ hasPart
```

但还缺一半：

* ❌ Topic → Program : isPartOf
* ❌ Tag → Program : isPartOf

否则会出现**反向污染 Program 主权**。

👉 **结论**：
必须补，不补是漏洞。

---

## 四、最终裁决（是否可以执行 07-01*）

### ❌ 原样执行 07-00*

不建议
→ 会误杀 L3
→ lint 职责略越界

### ✅ **推荐执行方案（你现在就可以动手）**

> **按下表调整后，07-01* 可安全执行并接 CI**

| 项   | 动作        |
| --- | --------- |
| L1  | 保留，ERROR  |
| L2  | 保留，ERROR  |
| L3  | 降级为 WARN  |
| P1  | 保留，ERROR  |
| C1  | WARN（可升级） |
| UI1 | 保留，ERROR  |
| N1  | 新增，ERROR  |
| N2  | 新增，ERROR  |

---

## 五、一句话总结（给你决策用）

> **07-01* 可以执行，但必须“减 1、加 2、调 1 级”。**
> 调完后，这套 aeo-lint 才是真正和你 `/CLAUDE.md + Guard` 体系对齐的“工程级执行器”。
