好，这一步你提得**非常专业**。
我已经按你当前 **`/CLAUDE.md (v2.0.1)` 的真实结构**，把 **aeo-lint README 中的每一条规则**，**反向精确映射到宪法条款编号**，形成**可审计链（Audit Trail）**。

下面这份内容你可以 **直接整体追加** 到：

```
tools/aeo-lint/README.md
```

作为 **「Appendix · Constitutional Mapping」终态附录**。

---

# Appendix A — aeo-lint ↔ CLAUDE.md 宪法条款映射（审计链）

> 本附录用于说明：
> **aeo-lint 中的每一条规则，源自 /CLAUDE.md 的哪一条宪法约束。**
>
> 原则：
>
> * aeo-lint **不产生新规则**
> * aeo-lint **仅执行 /CLAUDE.md 已存在的红线**
> * 任一 ERROR 级规则，必须能反向定位到明确宪法条款

---

## 一、ERROR 级规则映射（结构必然错误）

### 🔴 L1 — Level Canonical URL / 禁止 query / hash

**aeo-lint 规则说明**

* 禁止 `?stage=` / `#stage` 表达 Level
* 仅允许 `/levels/{beginner|intermediate|advanced}`

**宪法来源**

* `/CLAUDE.md`

  * **§3 Routing & URL Canonical Rules**

    * *Canonical Rules*

      > 每个实体只有一个 canonical URL
      > Slugs MUST NOT encode transient UI state
  * **§2.3 Naming & Enumeration Rules**

    * StageKey Enumeration（唯一合法枚举）

**审计结论**
→ 属于 **URL 层级 + 枚举主权** 的结构性红线
→ 命中即代表语义污染
→ **ERROR 合法**

---

### 🔴 L2 — JSON-LD educationalLevel 必须使用 DefinedTerm + /levels/*

**aeo-lint 规则说明**

* 禁止 string / enum
* `@id` 必须指向 `/levels/*`

**宪法来源**

* `/CLAUDE.md`

  * **§4 Data & Schema Rules**

    * *educationalLevel 使用规则*

      > Must use DefinedTerm
      > Must reference level entity
  * **§2.3 Naming & Enumeration Rules**

    * StageKey Enumeration（唯一真源）

**审计结论**
→ 属于 **Schema 表达真实性 + Level 主权**
→ 结构上不可能正确
→ **ERROR 合法**

---

### 🔴 P1 — Program / Course 教学结构主权

**aeo-lint 规则说明**

* Course ❌ `hasPart`
* Program ✅ `hasPart`
* Topic / Tag ❌ `hasPart`

**宪法来源**

* `/CLAUDE.md`

  * **§2.1 Page Responsibility Definitions**

    * Course

      > MUST NOT 定义学习顺序
    * Program

      > 用于表达先学什么 → 再学什么
  * **§2.2 Entity Relationship Rules**

    * Mandatory / Forbidden Relationships

**审计结论**
→ 属于 **教学结构主权不可坍塌点**
→ 一旦越权，知识图谱失效
→ **ERROR 合法**

---

### 🔴 UI1 — UI 状态不得进入语义层（JSON-LD / canonical）

**aeo-lint 规则说明**

* JSON-LD 中出现 `stage / tab / filter`
* canonical URL 含 query

**宪法来源**

* `/CLAUDE.md`

  * **§3 Routing & URL Canonical Rules**

    * Slugs MUST NOT encode transient UI state
  * **§4 Data & Schema Rules**

    * Schema 真实表达原则

**审计结论**
→ 属于 **UI 表现层 vs 语义层强隔离**
→ 搜索引擎层不可逆
→ **ERROR 合法**

---

### 🔴 N1 — Level @id 跨语言稳定性

**aeo-lint 规则说明**

* 禁止 `/zh/levels/*`、`/en/levels/*`
* Level `@id` 必须跨语言稳定

**宪法来源**

* `/CLAUDE.md`

  * **AI 宪法级总 Guard（Level / Program / Course）**

    * 条款 13：

      > Level @id must remain stable across languages
  * **§2.3 Naming & Enumeration Rules**

**审计结论**
→ 属于 **实体身份稳定性**
→ 搜索引擎与知识图谱硬约束
→ **ERROR 合法**

---

### 🔴 N2 — Topic / Tag 禁止 isPartOf Program

**aeo-lint 规则说明**

* Topic / Tag ❌ `isPartOf` Program

**宪法来源**

* `/CLAUDE.md`

  * **§2.2 Entity Relationship Rules**

    * Forbidden Relationships
  * **Appendix · Topic Hub（/t/）**

    * 不得替代 Program 结构主权

**审计结论**
→ 属于 **Program 结构主权防反向污染**
→ **ERROR 合法**

---

## 二、WARN 级规则映射（结构风险提示）

### 🟡 L3 — Level 中心性（入链检查）

**aeo-lint 规则说明**

* Level 页面至少应被一个非 Course 页面链接

**宪法来源**

* `/CLAUDE.md`

  * **§2.1 Page Responsibility Definitions**

    * Levels

      > Level 是阶段定义页，不是孤立节点
  * **§3 Routing & URL Hierarchy**

**审计结论**
→ 属于 **结构完整性建议**
→ 非必然错误
→ **WARN 合法**

---

### 🟡 C1 — Course 原子性信号（position / step / 阶段）

**aeo-lint 规则说明**

* Course 中出现顺序 / 阶段语义信号

**宪法来源**

* `/CLAUDE.md`

  * **§2.1 Page Responsibility Definitions**

    * Course MUST NOT 定义学习顺序
  * **§5 Coding Philosophy**

    * 禁止混合语义职责

**审计结论**
→ 属于 **语义越界高风险信号**
→ 需人工判断
→ **WARN 合法**

---

## 三、规则完整性声明（终态）

> **aeo-lint 中不存在任何一条“宪法外规则”。**
>
> 每一条 ERROR / WARN：
>
> * 均可反向定位到 `/CLAUDE.md` 的明确条款
> * 均不引入新的语义主权
> * 均可被人类与 AI 审计、复核、追责

---

### In short（中文一句话）

> **这份映射表把 aeo-lint 的每一条规则，牢牢锚定在 /CLAUDE.md 的具体条款上，形成了完整、可审计、不可漂移的语义执行链。**
