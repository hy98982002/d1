

## 三、关键问题：

### 怎么把这些文档“引入宪法”，但不污染宪法？

答案只有一句话：

> **在 `/CLAUDE.md` 里只“声明它们存在 + 职责边界”，
> 不复制内容、不总结内容、不解释内容。**

你现在的宪法已经有「文档分工总览」，
但可以**再强化 1 个小节**，让 ClaudeCode “天然知道该看什么”。

---

## 四、你应该如何改 `/CLAUDE.md`（最小、安全修改）

### ✅ 建议新增一个 **非常短** 的小节（不动正文规则）

位置建议：
`## 0. Document Governance & Reading Guide` 后面

### 建议补充内容（示例）

```md
### AI Working Context Map（AI 工作上下文地图）

在执行任何任务前，AI Agent 应理解以下文档的**职责差异**：

- `/CLAUDE.md`  
  → 唯一宪法。定义语义主权、实体关系、AEO 红线。  
  → 不解释实现，不描述工程现实。

- `/frontend/CLAUDE.md`  
  → 前端执行细则。说明在前端“如何实现”宪法规则。  
  → 仅约束前端，不得扩展或修改宪法语义。

- `/AI_EXECUTION_CONTEXT.md`  
  → AI 理解工程现实的背景材料。  
  → 描述当前代码结构、历史迁移、现实假设。  
  → **不具备任何约束力，不得作为规则依据。**

- `/DEPLOY_NOTES.md`  
  → 部署、构建、环境与运维说明。  
  → 与语义、AEO、实体规则无关，不得反向影响宪法判断。

AI Agent 在发生冲突时：
**必须以 `/CLAUDE.md` 为唯一裁决来源。**
```

👉 **这段话只做一件事**：
让 AI 在脑子里形成“文件权重图”。

---

## 五、给 Trae 的【专用提示词】——你可以直接用

下面这段是**你现在就可以复制给 Trae 的版本**：

```md
请你协助我对项目根目录的 `/CLAUDE.md` 做一次“最小侵入式补充”。

目标：
- 不修改任何既有宪法规则
- 不重写、不总结、不重复任何已有条款
- 仅补充一段“AI 工作上下文地图”，用于说明以下文档的职责边界与优先级：

  - /frontend/CLAUDE.md
  - /AI_EXECUTION_CONTEXT.md
  - /DEPLOY_NOTES.md

约束条件（非常重要）：
1. 新增内容必须是**声明性、说明性**语言，不得引入新的规则、禁止项或 MUST/SHOULD。
2. 不得复制上述文档中的具体内容，只能描述“它们是做什么的”。
3. 必须明确：只有 /CLAUDE.md 本身具备裁决权，其它文档均不得覆盖或修改宪法语义。
4. 修改应控制在一个小节内，不得影响现有章节结构。

输出要求：
- 给出建议插入的位置
- 给出完整 Markdown 文本
- 不对宪法正文做任何语义调整
```

---

## 最终一句判断（很重要）

你现在做的事情，本质上是：

> **在为 AI 建立“法典 + 说明书 + 背景资料 + 操作手册”的分权体系**

这不是洁癖，是**只有复杂项目才会遇到的治理问题**。

你现在这个状态：

* 宪法不被污染
* AI 不被现实绑架
* 前端可渐进修正
* 后端未来可无缝接入

**这是一个非常健康的工程状态。**


