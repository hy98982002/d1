好，下面这是一份**已经“压缩到可直接写入 AI_EXECUTION_CONTEXT.md 的长期记忆声明”**，并且**已经加工成「给 trae 的修改提示词」**。
这是**冻结级文本**，不是讨论稿。

---

## ✅ 给 trae 的修改提示词（RBAC 长期记忆声明 · 宪法兼容版）

> **请严格按以下指令修改 `AI_EXECUTION_CONTEXT.md`。
> 本次修改目标是：固化“RBAC 不适用”的长期工程判断，防止未来 AI 或人类重复引入过度设计。
> 不得新增分析性内容，不得扩展为规范。**

---

### 一、插入位置（非常重要）

请在 `AI_EXECUTION_CONTEXT.md` 中新增一个**独立小节**，推荐位置为：

```
## 5. 工程中的现实依赖假设
```

之下，新增：

```
### 5.x RBAC 使用判断（长期工程结论）
```

（x 取合适的顺序号，不要打乱现有结构）

---

### 二、写入的【长期记忆声明正文】（原文照抄）

请将以下内容 **逐字写入**，不要改写语气，不要扩展：

```md
### RBAC 使用判断（长期工程结论）

**当前结论**：在现有项目阶段与体系结构下，不引入 RBAC（Role-Based Access Control）。

**原因说明**：
- 当前项目的权限需求本质为“内容访问控制”，而非“操作权限控制”。
- 用户差异主要体现在身份状态（免费用户 / 单次购买用户 / 会员用户），而非多角色行为差异。
- 课程访问规则由课程元数据（Level、Access 条件、购买状态）与用户状态共同决定，业务语义清晰且可显式表达。
- 引入完整 RBAC 体系将增加不必要的抽象层，削弱业务规则的可读性与可维护性，属于过度设计。

**当前采用策略**：
- 使用基于用户状态与课程属性的显式访问判断逻辑（如 `canAccessCourse(user, course)`）。
- 权限判断集中在明确的业务函数中，而非通过角色-权限映射间接表达。

**RBAC 启用的硬性前提条件**（需同时满足至少两项）：
- 出现 5 种及以上长期并存的系统角色（如教师、助教、运营、审核、企业管理员等）。
- 出现明显的操作级权限差异（创建、编辑、审核、发布、回滚等）。
- 引入多租户企业空间或企业级管理后台。
- 后台权限系统成为主要复杂度来源。

在未满足以上条件前，禁止引入完整 RBAC 体系。
```

---

### 三、语义与风格约束（必须遵守）

* 本段内容属于 **工程现实判断**，不是规范，不得使用 `MUST / FORBIDDEN` 等宪法级措辞
* 不得在 CLAUDE.md 中重复或升级此判断
* 不得在 snippets 中引用此判断
* 本结论用于 **防止未来 AI 或人类重复引入 RBAC 过度设计**

---

### 四、完成后输出要求

修改完成后，请仅输出：

1. 新增的小节标题
2. 确认是否 **仅新增内容，未修改原有判断**
3. 不要输出解释性文字

---

## 🧠 这段话的真实作用（你心里要清楚）

这不是“现在不做 RBAC”的临时决定，而是：

> **“RBAC 在什么条件下才被允许出现”的工程宪法下位冻结声明**

未来任何 AI / 人想加 RBAC，**必须先推翻这段话**，否则就是违背工程记忆。

---

### In short（中文极简总结）

**这段提示词会把“当前禁止引入 RBAC + 何时才允许引入”冻结进 AI_EXECUTION_CONTEXT，防止未来过度设计反复发生。**
