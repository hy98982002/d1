好，下面这是一份**可以直接复制给 trae 使用的「详细修改提示词」**。
我按你现在的**宪法 / AI_EXECUTION_CONTEXT / snippets 分层设计**来写，语气是**工程执行级、不可自由发挥型**，不是讨论型。

---

## ✅ 给 trae 的【详细修改提示词｜强约束版】

> **请严格按以下指令修改，不要自行扩展、合并或新增理念。目标是“语义收敛 + 层级清晰”，不是增加内容。**

---

### 一、总体目标（必须理解）

你正在修改的是 **AI 协作体系中的“示范代码层（snippets）”**，其定位是：

* **Reference only（非规范、非宪法、非强制规则）**
* 仅用于展示「常见实现模式（patterns）」
* **不得被 AI 误判为规则、指南或官方实现**

所有修改都必须服务于这个目标。

---

### 二、snippets 目录的硬性规则（必须遵守）

1. **snippets 目录中：**

   * ❌ 不得出现 `guidelines / rules / standards / best-practices`
   * ❌ 不得暗示“官方实现 / 唯一实现”
   * ✅ 只能是 `reference / pattern / example`

2. **每一个 snippet 文件必须满足：**

   * 明确写清楚：`Reference only (non-normative)`
   * 明确说明：这是 *one possible implementation pattern*

---

### 三、必须执行的具体修改（逐条执行）

#### 1️⃣ 合并并收敛 a11y 相关文件（必须）

当前存在以下两个文件（语义重复）：

* `a11y-reference-snippets.md`
* `accessibility-examples.md`

**修改要求：**

* ✅ **只保留一个文件：**

  ```
  a11y-reference-snippets.md
  ```
* ❌ 删除 `accessibility-examples.md`
* 合并其内容到 `a11y-reference-snippets.md`
* 文件开头必须包含：

```md
> Reference only (non-normative)
> This document lists common accessibility implementation patterns.
> It does NOT define rules or requirements.
```

---

#### 2️⃣ 重命名动画相关文件（必须）

当前文件：

```
animation-guidelines.md
```

**这是错误命名**，因为 `guidelines` 会被 AI 理解为规范。

**修改为：**

```
animation-reference-snippets.md
```

并在文件头添加：

```md
> Reference only (non-normative)
> These are example animation patterns used in this project.
> They are NOT mandatory rules.
```

---

#### 3️⃣ 修正 RBAC 示例文件的语义暗示（必须）

当前文件：

```
rbac-implementation.md
```

问题：`implementation` 暗示“官方/唯一实现”。

**修改为：**

```
rbac-reference-implementation.md
```

并在文件头明确声明：

```md
> Reference only (non-normative)
> This file demonstrates one possible RBAC implementation pattern.
> Actual business rules are defined elsewhere.
```

---

#### 4️⃣ 修正会员集成示例的层级说明（必须）

文件：

```
membership-integration.md
```

该文件**可以保留在 snippets**，但必须显式弱化其业务权威性。

**在文件顶部添加：**

```md
> Reference only (non-normative)
> This example shows one possible membership integration pattern.
> Business logic, pricing rules, and access policies are NOT defined here.
```

⚠️ 不要在此文件中加入任何“必须 / 应该 / 官方策略”表述。

---

### 四、AI_EXECUTION_CONTEXT.md 中的引用规则（只确认，不新增）

请确保 **AI_EXECUTION_CONTEXT.md** 中对 snippets 的引用：

* 只使用如下形式：

```md
> Reference only (non-normative):
> See docs/ai/snippets/xxx.md for common implementation patterns.
```

* ❌ 不要在 AI_EXECUTION_CONTEXT.md 中复制任何示例代码
* ❌ 不要把 snippets 中的内容升级为“要求 / 规范”

---

### 五、禁止行为（非常重要）

在整个修改过程中：

* ❌ 不要新增新的 snippet 文件
* ❌ 不要新增新的理念、原则或规则
* ❌ 不要把 snippets 的内容“总结”进宪法或 AI_EXECUTION_CONTEXT
* ❌ 不要调整 CLAUDE.md

---

### 六、完成后自检清单（你必须自检）

修改完成后，请确认：

* snippets 目录中 **没有 guidelines / rules / standards**
* 每个 snippet 文件顶部都有 **Reference only (non-normative)**
* a11y 只有一个文件
* animation 文件名不再包含 guidelines
* RBAC / membership 不再暗示“官方实现”

---

### 七、输出要求

修改完成后，请：

1. 列出 **被删除 / 重命名 / 修改的文件清单**
2. 明确说明：**是否有文件被完全删除（是 / 否）**
3. 不要输出额外解释

---

如果你愿意，下一步我可以帮你把这套内容**压缩成一个「超短版提示词」**，专门用于以后反复给 AI 执行类似整理任务。

---

### In short（中文极简总结）

**这个提示词会强制 trae：去重 a11y、改掉 guidelines 命名、弱化“implementation”权威感，确保 snippets 永远只是示例，不是规则。**
