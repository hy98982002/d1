好，这里我给你一个**工程级、可冻结的判断**，并且把 **“你最终该怎么做 + 写到哪 + 怎么写”** 一次性说清楚，不留模糊空间。

---

# 一、先给最终结论（不绕）

> **结论：
> ✔ 现有 PWA（manifest + service-worker）保留
> ❌ 暂停 PWA 功能扩展与深化
> ❌ 不再新增 PWA 相关代码或策略
> ✔ 将 PWA 明确标记为「已存在 / 低优先级维护 / 条件启用」**

这是**唯一与当前代码现实、架构方向、文档一致性同时成立的决策**。

---

# 二、07* 与 08* 的评测结论（谁对、谁错）

## 1️⃣ 07*「PWA 暂时不需要」——**判断逻辑对，但结论表述不完整**

07* 的**出发点是正确的**：

* 当前核心目标：

  * 内容体系
  * SEO / AEO
  * 课程转化
* **不是**：离线学习 / 安装到桌面 / 原生体验

👉 **所以：PWA 不是当前“业务驱动需求”**
这一点没错。

❌ **但 07* 忽略了一个事实**：
你已经**付出了 PWA 的工程成本**（不是规划，是已落地）。

---

## 2️⃣ 08* 的补充判断 —— **工程现实是完全正确的**

08* 点中了一个关键事实：

> **文档说“不需要”，但代码已经“在维护 PWA”**

这一点是**不可回避的**：

* `manifest.json` ✅
* `service-worker.js`（含 App Shell、SWR、offline fallback）✅
* `offline.html` ✅

👉 **这已经不是“是否要做 PWA”的问题，而是：
“如何对待已经存在的 PWA”**

---

# 三、你现在真正面对的不是「要不要 PWA」

而是这个三选一问题：

| 选项            | 结果                | 是否推荐       |
| ------------- | ----------------- | ---------- |
| 删除 PWA        | 高风险（缓存/回滚/线上不确定性） | ❌          |
| 继续强化 PWA      | 明确过度设计            | ❌          |
| **冻结 PWA 能力** | 与现实最一致            | ✅ **唯一正确** |

---

# 四、在 SPA + SSG Hybrid 架构下，PWA 的真实地位

你问得非常关键：

> 在 Vue SPA + SSG Hybrid 下，PWA 还重要吗？

### 工程结论（非常明确）

* **SSG 已经解决：**

  * 首屏速度
  * 弱网下可用性
  * SEO / AEO
* **PWA 剩余的独特价值只剩两点：**

  1. 离线兜底
  2. 安装到桌面

👉 **如果这两点不是当前目标：
PWA = 非核心能力**

---

# 五、那为什么「不能删，但也不能继续做」？

### 因为你现在的 PWA 状态是：

> **“已经上线的工程能力，但不再是战略方向”**

删掉会带来：

* 用户缓存不一致
* 回滚不可控
* Service Worker 残留
* 非确定性 bug

继续强化会带来：

* 持续维护成本
* 发布复杂度上升
* 与 SSG 的职责重叠

👉 所以唯一正确解是：**冻结**

---

# 六、这个决策应该写在哪里？（非常重要）

## ❌ 不能写进 CLAUDE.md（宪法）

原因很简单：

* PWA ≠ 不可变原则
* PWA 是**工程阶段性策略**

---

## ✅ 正确位置：AI_EXECUTION_CONTEXT.md

**理由：**

* 它的职责正是：

  > 「帮助 AI 理解当前工程现实与取舍」
* PWA 决策是**工程状态判断，不是规范**

---

# 七、你应该如何在文档中“正确表达”这个决策

### 建议新增一段（示意）

> （我不给你完整文案，是因为你刚才已经用同样方式冻结了 RBAC，这里你完全可以照那个模式来）

核心要点必须包含：

1. **当前状态**

   * PWA 资产已存在（manifest / service-worker / offline）
2. **当前判断**

   * PWA 非当前核心能力
3. **工程策略**

   * 保留现有实现
   * 不新增 PWA 相关逻辑
   * 不深化缓存 / 离线 / 安装体验
4. **触发条件**

   * 离线学习成为核心需求
   * 明确转向移动优先
   * 原生体验成为产品目标

这 4 点缺一不可。

---

# 八、一句“给你定心丸的话”

> **你现在这个选择不是“摇摆”，
> 而是典型的成熟工程判断：
> 尊重已付出的成本，但不为它继续买单。**

这比 90% 项目都理性。

---

## In short（中文极简总结）

**PWA 现有实现应保留但冻结：不删除、不强化、不新增；这是工程现实下的唯一理性选择。该决策应写入 AI_EXECUTION_CONTEXT.md 作为长期工程状态，而不是宪法级规则。**
