好，这一步我会**非常克制、非常工程化**地帮你完成。
你现在要的不是“好看”，而是**让 Trae 一次就写对结构、不跑偏、不擅自扩展**。

我会给你两样东西：

1️⃣ **一个可以直接丢给 Trae 的【单一 Prompt】**（只生成 **一个完整 HTML 单页结构**）
2️⃣ **这个单页在产品语义上的“设计解释图”（文字版 + 结构示意）**
👉 生图我**不在这一轮直接生成**，原因我会解释清楚（这是为了不污染 Trae 的结构判断）

---

## 一、给 Trae 的【唯一 Prompt｜直接复制用】

> ⚠️ 这个 Prompt 是**强约束版**，目的只有一个：
> **生成一个“会员专区页”的完整 HTML 结构示意**
> 不写 Vue 逻辑、不写 JS、不拆组件、不做路由
> 👉 只输出 **一个可阅读、可讨论、可评审的单页骨架**

---

### ✅ Trae Prompt（请原样复制）

```
你现在是一个前端架构工程师。
请只生成【一个】完整的 HTML 页面，用于展示「Doviai · 会员专区页」的结构示意。

⚠️ 严格约束（非常重要）：
1. 只生成一个 HTML 文件（<html>…</html>）
2. 不要写 Vue 逻辑、不用 <script>、不用组件拆分
3. 不使用任何 JS
4. 不追求视觉样式，只用语义化 HTML + class 命名表达结构
5. 所有内容为“结构占位 + 说明性文案”，不是最终文案
6. 不要自行新增业务模块，必须严格按下述结构

——————————————————
【页面定位】
这是「会员专区页」，不是课程列表页。
核心目的：让会员看到“学习系统与路径”，课程只是系统的组成部分。

——————————————————
【页面结构必须严格包含以下 4 个区块，顺序不可变】

=== Section 1：会员学习系统总览（核心区）===
语义目的：这是会员真正购买的“系统感”

内容必须包括：
- 当前阶段（例如：判断力重建 · Stage 1）
- 当前主线目标（一句话）
- 系统判断状态（例如：你没有走弯路）
- 一个主 CTA（例如：进入我的学习路径）

请使用 <section> + 明确 class 命名，如：
.section-system-overview

——————————————————
=== Section 2：当前学习路径（系统主线）===
语义目的：展示“你现在在走哪条路”，不是所有可能性

内容必须包括：
- 路径名称
- 路径说明（为什么是这条）
- 当前所处节点（文本说明即可）
- 下一步节点（文本说明即可）

请明确这是“系统路径”，不是课程列表

——————————————————
=== Section 3：系统模块课程（会员专享课程）===
语义目的：课程是“系统模块”，不是卖点本身

内容必须包括：
- 模块标题（例如：判断力重建 · 系统模块）
- 至少 3 个课程卡片占位（课程名用占位）
- 每个课程卡片必须包含一句：
  “系统为什么在这个阶段给你这门课”

注意：这里不是课程商城，不要出现价格、评分、销量

——————————————————
=== Section 4：会员说明与心理确认区（被鼓励）===
语义目的：强化“慢是对的”“你没走偏”

内容必须包括：
- 一段明确的鼓励式说明文案
- 明确表达：
  - 不需要学更多
  - 当前阶段不追求速度
  - 系统会替用户兜底方向

——————————————————
【技术与输出要求】

- 使用语义化标签：<header> <main> <section> <article> <footer>
- class 命名要体现产品语义，而不是样式
- 页面标题请使用：Doviai 会员专区 · 学习系统
- 所有文案为结构示意，不要写营销文案
- 不要输出解释文字，只输出 HTML
```

---

## 二、这个单页在产品上的“真实含义”（你心里必须非常清楚）

这个 HTML 页 **不是 UI**，而是：

> **会员专区的“产品宪法页”**

它在做三件事：

1. **把“会员 ≠ 课程”这件事结构化**
2. **强制所有人（包括 AI）理解：系统 > 内容**
3. 为你后续拆 Vue 组件、RBAC、权限 gating 提供母板

---

## 三、这个页面未来如何演化（你现在不用做，但要心里有数）

| 当前      | 后续               |
| ------- | ---------------- |
| 单一 HTML | 拆成 Vue Sections  |
| 无逻辑     | 接入 membership 状态 |
| 文本判断    | 接 AI 学习建议        |
| 静态路径    | 动态 Path 实体       |

**但现在一律不做。**

---

## 四、为什么我不建议“现在就生图”（重要解释）

你刚才说：

> “你也可以给我用你生图的方式生成一个完整单页示意图”

我很清楚你为什么想要，但我**刻意不在这一轮生成**，原因只有一个：

> **一旦你先看到“图”，
> Trae 和你都会不自觉开始围绕“样式”而不是“结构”讨论。**

你现在处在的是：

* **体系冻结期**
* **结构定型期**
* **Guard 建立期**

👉 **这个阶段，图是噪音**

---

## 五、正确顺序（非常重要）

请按这个顺序走：

1️⃣ 用上面的 Prompt 让 Trae 生成 **HTML 单页结构**
2️⃣ 你和我一起评审：

* 是否“系统感”足够强
* 是否真的不是“课程页”
  3️⃣ **结构定稿后**
  👉 我再帮你用生图方式生成 **“高保真单页示意图”**（那时才有意义）

---

## 六、下一步你可以直接做的事

你现在可以马上：

* 把上面的 Prompt 丢给 Trae
* 把生成的 HTML 粘回来给我

我可以下一步帮你做：

* 🔍 **结构审计（是否还残留“卖课思维”）**
* 🧱 **拆成 Vue 模块清单（精准到文件级）**
* 🎨 **在不破坏结构的前提下出视觉方案**
* 🧠 **同步生成对应的 JSON-LD / AEO 结构**

你先跑 **第 1 步**。
等你把 Trae 的 HTML 给我，我们继续。
