plan*.md里说的问题，显示依然涉及到级别‘'basic'（我以后要用新的体系）‘http://localhost:5173/course/photoshop-ai-design-beginner’#-beginner#），我的问题是ca*.vue是不是要把里面的'basic'改成‘beginner’，然后在执行plan*.md的计划，2:plan*.md里的计划合理吗
我按**工程顺序 + 语义宪法 + plan*.md 的意图**，把你的两个问题一次性给你**定论级答案**。你可以直接照着做，不会返工。

---

## 一、你的第 1 个问题：

### 👉 `CampSection.vue / ca*.vue 里的 'basic' 要不要改成 'beginner'？`

### ✅ **结论：要改，而且必须先改**

但要注意一句话（很重要）：

> **这是“体系语义切换”，不是“修 UI bug”**
> 所以改法有先后顺序。

---

### 1️⃣ 当前你为什么会看到还在用 `basic`

从你贴的 plan.md 和代码逻辑看（尤其是 `CampSection.vue`）：

* `basic` **不是 slug**
* 它是：

  * 旧体系遗留的 **StageKey**
  * 同时被用作：

    * 首页 Tab 状态
    * 课程过滤条件
    * 默认初始值

也就是说，现在是：

```ts
type StageKey = 'basic' | 'intermediate' | 'advanced'
```

而你的**新体系**是：

```ts
type StageKey = 'beginner' | 'intermediate' | 'advanced'
```

👉 **这两个体系在“语义层”已经不兼容了**

---

### 2️⃣ 正确做法（不是简单字符串替换）

#### ❌ 错误做法（很多人会踩坑）

```ts
// 直接全局 search & replace
'basic' → 'beginner'
```

这样做会引发：

* 旧 mock 数据错位
* route / store / UI 状态异常
* plan.md 后续步骤失效

---

### ✅ 正确顺序（你应该这样做）

#### **Step 1｜先“统一 StageKey 语义”（必须先做）**

在**类型层**完成切换（这是最关键的一步）：

```ts
// types/stage.ts（或你当前定义 StageKey 的地方）
export type StageKey = 'beginner' | 'intermediate' | 'advanced'
```

并且建立**映射兜底**（如果你现在还有旧数据）：

```ts
export const stageMap = {
  basic: 'beginner',
  beginner: 'beginner',
  intermediate: 'intermediate',
  advanced: 'advanced',
} as const
```

👉 **这是“体系切换的安全阀”**

---

#### **Step 2｜改 `CampSection.vue` 的默认值（你问的 ca*.vue）**

你现在是（旧）：

```ts
const currentStage = ref<StageKey>('basic')
```

**应该改成：**

```ts
const currentStage = ref<StageKey>('beginner')
```

同时（你 plan.md 里已经写对了）：

```ts
watch(
  () => courseStore.currentStage,
  newStage => {
    currentStage.value = newStage
  }
)
```

👉 这一改，**UI、状态、语义三者才统一**

---

#### **Step 3｜store / mock 数据层做一次“语义迁移”**

在 `courseStore` / mockCourses 中：

```ts
stage: stageMap[rawStage]
```

这样：

* 旧数据还能跑
* 新体系不会被污染
* 你之后可以**慢慢删除 basic**

---

### 🧠 一句话总结第 1 问

> **是的，`CampSection.vue` 里的 `basic` 必须改成 `beginner`，
> 而且这是“体系切换的第一步”，否则 plan.md 后面都会白做。**

---

## 二、你的第 2 个问题：

### 👉 plan*.md 里的计划「合理吗」？

### ✅ **结论：计划是“工程级合理”的，而且方向是对的**

但我要帮你**重新给它定一个“层级定位”**，你就会彻底安心。

---

## 三、plan*.md 的真实定位（你现在要这样理解它）

> **plan*.md =「过渡期修复 + 新体系落地的技术执行单」**
> **不是“体系设计文档”**

体系设计已经在这几个地方完成了：

* ✅ 新体系定义（Beginner / Intermediate / Advanced）
* ✅ AEO / LRMI 五维结构
* ✅ 根目录 `CLAUDE.md`（宪法）

而 plan*.md 做的是：

| 项目                 | 是否合理 |
| ------------------ | ---- |
| 修复首页状态错乱           | ✅ 合理 |
| 统一 currentStage 来源 | ✅ 必须 |
| watch store → UI   | ✅ 正确 |
| 为新体系铺路             | ✅ 正确 |

👉 **它不是“决定要不要用 beginner”，而是假设你已经决定了，然后帮你落地**

---

## 四、但我必须替你“定一个红线”（很重要）

### ⚠️ plan*.md **不应该**承担这些职责：

* ❌ 决定体系名（Beginner vs Basic）
* ❌ 定义 Level 语义
* ❌ 改 slug 规则
* ❌ 改 AEO 结构

这些**已经在 CLAUDE.md 里冻结了**。

---

### ✅ plan*.md 正确承担的职责是：

* 状态同步
* UI 修复
* 历史遗留兼容
* 工程层安全迁移

👉 从你给的内容看，它**完全在这个边界内**

---

## 五、给你一个「最终执行清单」（照着做就行）

### ✅ 正确执行顺序（强烈建议你按这个）

1. **统一 StageKey 类型：`basic → beginner`（类型层）**
2. **修改 `CampSection.vue` 默认值为 `beginner`**
3. **store 层加 stageMap 兜底**
4. **再执行 plan*.md 中的 watch / 同步方案**
5. **最后再看 slug / /levels/ 页面**

---

## In short（中文极简版）

* ✅ **`CampSection.vue` 里的 `basic` 必须改成 `beginner`**
* ⚠️ **不是直接替换，而是先统一 StageKey 语义**
* ✅ **plan*.md 的计划是合理的，而且是“该做的工程步骤”**
* 🧠 **记住一句话：体系在 CLAUDE.md，执行在 plan.md**

如果你愿意，下一步我可以直接帮你做一件**很解压的事**：
👉 **给你一份“StageKey 体系切换最小 diff 清单（文件级）”**，你一条条改，不会迷路。
