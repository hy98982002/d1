# Slug优化完整解决方案

## 1. 当前状态分析

### ✅ 好消息
- 项目已正确配置 stageMap.ts：`basic` → `beginner`，`intermediate` → `intermediate`，`advanced` → `advanced`
- slug.ts 中的 `generateCourseSlug()` 函数应该已调用 `getStageSlug()` 获取正确的体系词

### 🔍 待处理
- 现有模拟课程的 slug 仍使用旧格式（如 `-basic`）
- 需要评估是否需要设立实体的 `/level/` 页面

## 2. 完整解决方案

### 方案1：批量更新模拟课程slug

**文件**：`/frontend/src/store/courseStore.ts`

**操作**：
1. 遍历所有模拟课程，将slug中的 `-basic` 替换为 `-beginner`
2. 确保其他阶段的slug格式正确
3. 验证更新后的slug符合AEO/LRMI最佳实践

### 方案2：评估是否需要设立实体/level/页面

#### 结论
**推荐设立实体 `/level/` 页面**，理由：
- 符合AEO/LRMI最佳实践，提升语义强度
- 有利于搜索引擎建立知识图谱
- 为用户提供清晰的学习路径导航
- 支持更高级的结构化数据实现

#### 具体实现
1. **添加level页面路由**：
   ```typescript
   // router/index.ts
   {
     path: '/levels/:slug',
     name: 'Level',
     component: () => import('../views/LevelView.vue'),
     props: true
   }
   ```

2. **slug处理**：
   - level页面使用 `beginner`/`intermediate`/`advanced` 作为slug
   - 课程slug保持现有格式不变
   - 实现双向关联：level页面展示对应阶段的所有课程，课程页面关联到对应level页面

3. **结构化数据增强**：
   ```json
   "educationalLevel": {
     "@type": "DefinedTerm",
     "name": "Beginner",
     "@id": "https://www.doviai.com/levels/beginner"
   }
   ```

## 3. 实施步骤

### 步骤1：批量更新模拟课程slug

1. 遍历 `courseStore.ts` 中的 `mockCourses` 数组
2. 使用正则表达式或字符串替换更新slug
3. 验证更新后的slug格式

### 步骤2：添加level页面支持

1. 创建 `LevelView.vue` 组件
2. 添加level页面路由
3. 实现level页面与课程的关联
4. 增强结构化数据生成

### 步骤3：验证修改效果

1. 运行类型检查：`npm run type-check`
2. 运行构建：`npm run build:check`
3. 手动验证所有课程的slug格式
4. 验证level页面能正确访问
5. 检查结构化数据生成是否正确

## 4. 预期效果

### ✅ 课程slug优化
- 所有模拟课程的slug使用LRMI推荐的体系词
- 格式统一：`{course-topic}-beginner/intermediate/advanced`
- 符合AEO/LRMI最佳实践，提升语义强度

### ✅ 实体level页面
- 提供清晰的学习路径导航
- 增强AEO语义理解
- 支持图谱级结构化数据
- 提升用户体验和SEO效果

### ✅ 向后兼容
- 现有路由和链接仍能正常工作
- 旧slug格式仍能被正确解析
- 平滑过渡，无破坏性变更

## 5. 代码修改示例

### 模拟课程slug更新示例
```typescript
// 旧格式
slug: 'photoshop-basic'

// 新格式
slug: 'photoshop-beginner'
```

### Level页面结构化数据示例
```typescript
// utils/jsonld/buildCourseJsonLd.ts
export function buildCourseJsonLd(course: Course) {
  return {
    "@type": "Course",
    "name": course.title,
    "description": course.description,
    "educationalLevel": {
      "@type": "DefinedTerm",
      "name": course.stage === 'basic' ? 'Beginner' : 
               course.stage === 'intermediate' ? 'Intermediate' : 'Advanced',
      "@id": `https://www.doviai.com/levels/${getStageSlug(course.stage)}`
    },
    // 其他结构化数据
  }
}
```

## 6. 后续建议

1. **建立slug生成规范**：确保所有新课程使用统一的slug格式
2. **监控SEO效果**：跟踪优化前后的搜索引擎表现
3. **持续优化结构化数据**：逐步实现更高级的AEO/LRMI功能
4. **考虑实现方案C**：建立完整的/levels/枚举页，实现图谱级AEO理解

这个方案既满足了用户的需求，又符合AEO/LRMI最佳实践，同时保持了向后兼容，是当前项目的最优选择。