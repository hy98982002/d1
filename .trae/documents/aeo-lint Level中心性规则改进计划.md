# 一、当前状态分析

## 1. aeo-lint现有状态
- **已实现Level中心性规则**：`level-centrality.js`（规则编号L3，WARN级别）
- **现有实现逻辑**：
  - 逐个文件扫描，检查Level页面是否包含被其他页面链接的证据
  - 检查重要页面（home/program/guide）是否链接到所有Level
  - 采用简化方法：检查单个文件中是否包含指向该Level的链接

## 2. 09*文件内容分析
- **提出Level中心性入链规则**：基于link graph的完整检测
- **建议实现逻辑**：
  - 全局扫描所有文件中的链接关系（`href`和`router-link to`）
  - 统计非Course页面到Level页面的入链数
  - 如果入链数为0，发出WARN警告
- **建议实现方式**：创建`level-link-graph.js`，包含`collect`和`check`方法

## 3. 对比与差距
| 对比项 | 现有实现 | 09*建议 |
|--------|----------|---------|
| 检测范围 | 单个文件 | 全局链接关系 |
| 链接识别 | 仅检查当前文件中的链接 | 扫描所有文件中的链接 |
| 入链统计 | 简化检查，准确性较低 | 精确统计非Course页面入链 |
| 实现方式 | 单文件检查 | collect+check双阶段检测 |
| 规则效果 | 基本覆盖 | 更准确、更全面 |

# 二、改进计划

## 1. 核心改进点
- **替换现有规则**：用09*建议的实现替换`level-centrality.js`
- **实现双阶段检测**：
  - `collect`阶段：收集所有链接关系
  - `check`阶段：分析链接关系，统计入链数
- **精确识别非Course页面**：确保只统计非Course页面到Level页面的入链

## 2. 具体实施步骤

### 步骤1：修改规则加载机制
- 更新`index.js`，支持collect+check双阶段检测
- 新增context对象，用于传递全局链接数据

### 步骤2：替换level-centrality.js
- 删除现有`level-centrality.js`
- 创建新的`level-centrality.js`（保留原文件名，使用09*建议的实现逻辑）
- 实现collect和check方法

### 步骤3：更新README.md
- 确保规则文档与新实现一致
- 验证规则说明是否准确反映实际检测逻辑

### 步骤4：测试验证
- 运行aeo-lint，验证新规则是否正常工作
- 检查输出是否符合预期

## 3. 预期效果
- **更准确的检测**：基于全局链接关系，避免单文件扫描的局限性
- **更全面的覆盖**：识别所有非Course页面到Level页面的入链
- **更清晰的输出**：统一WARN级别，明确提示结构性风险
- **保持规则一致性**：规则编号L3保持不变，便于跟踪和维护

# 三、决策建议

**结论**：现有aeo-lint已经包含Level中心性检测规则，但实现方式较为简化。09*建议的实现方式更为完善，能够更准确地检测Level页面的入链情况。

**建议**：
1. 采用09*建议的实现方式，改进现有Level中心性规则
2. 保留原规则编号L3，确保规则体系的连续性
3. 更新相关文档，确保与新实现一致

**不需要新增独立规则**，只需改进现有规则即可实现09*文件的要求。